services:
  backend:
    container_name: backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production #TODO fix an env file
      - DB_PATH=/app/database/transcendence.db
    ports:
      - 3000:3000
    volumes:
      - database:/app/database
    restart: always
    networks:
      - transcendence
    develop:
      watch:
        - action: rebuild
          path: ./backend/src
          target: /app/src

  frontend:
    container_name: frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile.real
    ports:
      - 80:80
      - "443:443"
    depends_on:
      - backend
    restart: always
    networks:
      - transcendence
    volumes:
      - ./frontend/nginx/certs:/etc/nginx/certs:ro

volumes:
  database:
    name: transcendence_database
    driver: local
    
networks:
  transcendence:
    driver: bridge
  
# to add in service: env file 